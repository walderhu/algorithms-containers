{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug",
            "program": "${workspaceFolder}/build/s21_containers_test", // Указан путь к вашему исполняемому файлу
            // "program": "${workspaceFolder}/build/s21_containers", // Указан путь к вашему исполняемому файлу
            "args": [], // Здесь вы можете указать аргументы командной строки, если они нужны
            "cwd": "${workspaceFolder}", // Рабочая директория
        }
    ]
}